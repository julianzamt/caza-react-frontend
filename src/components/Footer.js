import Typography from '@material-ui/core/Typography'

const Footer = () => {
    return (
        <div>
            <Typography color="primary" variant="h1">Footer!</Typography>
        </div>
    )
}

export default Footer